include "main.h"

/**
 * _islower - check for lower characters
 * returns 1 if alphabet is in lowecase
 * returns 0 otherwise
 */
int _islower(int c)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		if (c = 'a' && <= 'z')
		{
			return (1);
		}
		return (0);
	}
}
